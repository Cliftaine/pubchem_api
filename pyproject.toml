[project]
name = "pubchem-hazard-api"
version = "0.1.0"
description = "API to check chemical hazard information via PubChem"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "httpx",
    "pyyaml",
]
